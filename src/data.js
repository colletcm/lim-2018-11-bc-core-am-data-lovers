// esta es una función de ejemplo
// puedes ver como agregamos la función a nuestro objeto global window

/*const example = () => {
  return 'example';
};

window.example = example; */

/*let datos = LOL.data;

for (let i=0; i<datos.length; i++) {
  console.log(datos.id)
}; */

/*
-  Almacenar data del prompt
- Comparar valor del prompt con valor del objeto
- Si es igual, jalar dato del objeto ingresado
- Imprimir en consola
*/


/* const jugador = prompt("¿De que jugador quieres saber el key? (Azir, Aatrox, Ahri)");
let name = Object.keys(LOL.data);

//recorriendo campeones para averiguar el Key
for (let i = 0; i < name.length; i++) {
  if (name[i] === jugador) {
    let jugadorAnalizado = name[i];
    for (let y = 0; y < name.length; y++) {

    }
  }
}
*/